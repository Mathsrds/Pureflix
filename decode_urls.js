// decode_urls.js - Versão simplificada para URLs já decodificados
// Se os URLs no direct_links.json já estão em formato legível,
// esta função apenas retorna os dados sem transformação.

function decodeDirectLinks(raw) {
  // Os URLs já estão em formato decodificado/legível
  // Nenhuma transformação necessária
  return raw;
}
